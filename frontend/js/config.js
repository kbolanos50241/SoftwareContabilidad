/**
 * Configuración del frontend - Software Contabilidad
 * URL base del API (backend C# Web API)
 */
const API_CONFIG = {
    baseUrl: 'http://localhost:5222/api',  // Coincide con launchSettings del backend
    timeout: 10000
};
